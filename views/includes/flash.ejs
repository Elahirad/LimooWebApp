
<% success.forEach(msg => { %>
    <div class="alert alert-success text-center"><%= msg %></div>
<% }) %>

<% errors.forEach(err => { %>
    <div class="alert alert-danger text-center"><%= err %></div>
<% }) %>